<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Biblioteca Musical</title>
</head>
<body>
  <h1>🎵 Biblioteca Musical</h1>
  <form method="POST" action="/">
    <input type="text" name="query" placeholder="Buscar canción..." required>
    <button type="submit">Buscar</button>
  </form>
  <h2>Resultados:</h2>
  <% if (canciones.length > 0) { %>
   <ul id="resultados">
  <% canciones.forEach(c => { %>
    <li data-id="<%= c.id %>">
      <strong><%= c.title %></strong> - <%= c.channel %><br>
      <iframe width="320" height="180" src="https://www.youtube.com/embed/<%= c.id %>" frameborder="0" allowfullscreen></iframe><br>
      <button class="fav-btn">⭐ Agregar a favoritos</button>
    </li>
    <hr>
  <% }) %>
</ul>

<h3>Tus favoritos:</h3>
<ul id="favoritos"></ul>
  <% } else { %>
    <p>No se encontraron resultados.</p>
  <% } %>
</body>
</html>
