<!DOCTYPE html>
<html>
<head>
  <title>Biblioteca Musical</title>
  <style>
    body { font-family: Arial; padding: 20px; background: #f4f4f4; }
    h1 { color: #333; }
    iframe { margin-top: 10px; }
  </style>
</head>
<body>
  <h1>🎵 Biblioteca Musical</h1>

  <form method="POST" action="/">
    <input type="text" name="query" placeholder="Buscar canción..." required />
    <button type="submit">Buscar</button>
  </form>

  <h2>Resultados:</h2>
  <% if (canciones.length > 0) { %>
  <ul id="lista-videos">
    <% canciones.forEach((c, i) => { %>
        <li data-index="<%= i %>">
            <strong><%= c.title %></strong> - <%= c.channel %><br>
            <iframe 
                class="video-player"
                width="320" 
                height="180" 
                src="https://www.youtube.com/embed/<%= c.id %>?enablejsapi=1" 
                frameborder="0"
                allow="autoplay"
                allowfullscreen>
            </iframe>
        </li>
    <% }) %>
</ul>

<script src="/script.js"></script>
  <% } else { %>
    <p>No hay resultados aún.</p>
  <% } %>
</body>
</html>
