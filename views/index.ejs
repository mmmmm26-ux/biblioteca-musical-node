<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Biblioteca Musical</title>
</head>
<body>
  <h1>🎵 Biblioteca Musical</h1>
  <form method="POST" action="/">
    <input type="text" name="query" placeholder="Buscar canción..." required>
    <button type="submit">Buscar</button>
  </form>
  <h2>Resultados:</h2>
  <% if (canciones.length > 0) { %>
    <ul>
      <% canciones.forEach(c => { %>
        <li><%= c.title %> - <%= c.channel %>
        <a href="https://www.youtube.com/watch?v=<%= c.id %>" target="_blank">🎧</a></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No se encontraron resultados.</p>
  <% } %>
</body>
</html>
